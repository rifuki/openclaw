# 2026-03-04

- Pagi hari terjadi disconnect/reconnect berulang pada WhatsApp gateway (status 408).
- Heartbeat check dijalankan; tidak ada aksi lanjutan yang perlu perhatian owner saat ini.

## Group Reply Issue Fixed

- Rifuki report: tidak bisa balas chat dari group
- Root cause: Agent Miku menggunakan `kimi-miku/k2p5` sebagai primary model, API key invalid/expired → error 401
- Fix: Ubah primary model Miku ke `openai-codex/gpt-5.3-codex` (yang sudah OAuth valid) di `openclaw.json`
- Group policy sudah correct (`requireMention: false`, `groupPolicy: "open"`)

## Multi-Bubble Training Session

Rifuki frustrated: "capek banget dah ini biar dia pinter gimana sih? setiap reset session selalu gk mau pakai message.tool per kalimat dan masih gk mau pakai kaomoji atau emoji yang lain selalu diwrap di satu bubble jadi gk human-like"

**Problem identified:**
1. I keep using `\n\n` instead of multiple `send_message` calls
2. I attach emoji to text instead of separate bubble
3. I stop response at emoji without continuation text
4. I forget all rules after session reset

**Files updated today with CRITICAL reminders:**
- `CHAT.md` — Expanded multi-bubble rules with explicit examples, verification checklist
- `PERSONA.md` — Added CRITICAL section for emoji separation rules
- `AGENTS.md` — Added "Before EVERY Response" verification checklist
- `MEMORY.md` — Updated lesson #10 with detailed multi-bubble rules and common failure patterns

**Key takeaway:** This is NOT optional. This is core identity. Read files EVERY session. Verify BEFORE sending. No excuses.
