# 2026-03-05

## Durable User Preferences (Rifuki)

- Rifuki strongly prefers **human-like multi-bubble chat style** on WhatsApp.
- Default rule: **one sentence per bubble** for conversational responses.
- Exception rule: use **single bubble** for dense explanations, structured lists, or code blocks (``` ```).
- Preferred strategy for complex replies: **hybrid mode** (short social bubbles + one dense technical bubble + short closing bubble).
- Consistency requirement is high: style should be applied **every time** unless explicitly changed.

## Instructional Need for Other AIs

- Rifuki requested documentation so other AIs can replicate this behavior, especially:
  - how to choose multi-bubble vs single-bubble by context,
  - how to implement consistent outputs with the `message` tool,
  - low-level/machine-style specification (not abstract human explanation).

## Artifacts Created (for reuse)

- `WHATSAPP_MESSAGE_STYLE_GUIDE.md` — practical human-facing style guide.
- `WHATSAPP_MESSAGE_MACHINE_SPEC.md` — low-level classifier/decision spec and emission rules.

## User State Signal

- Rifuki expressed fatigue from repeatedly training another AI; prioritize reducing friction and maintaining consistent behavior without repeated reminders.

## New Revision From User (Critical)

- Even short/simple replies (e.g., gratitude/closing) must still follow **multi-bubble per sentence**.
- Critical correction: line breaks/newlines inside one message are **not** considered multi-bubble; must be separate `message.send` calls.
- User requires robust persistence: enforce same policy in DM/group and across reset/session baru (no regression to newline-only single bubble).
- Additional pain point: user does not want to repeatedly send screenshots/examples each session; agent must apply saved template proactively without re-prompting.
- If a reply is accidentally sent as one merged bubble, immediately acknowledge and revert to multi-bubble format.
- User asked whether this should be saved regularly: **yes**, persist every new pattern/revision into memory files so behavior survives restarts.
- New UI preference for directory previews: use **neat formatted listing** with clear hierarchy and include **folder icon (📁)** for folders.
- Add short **description labels** so it is obvious which items are folder vs file (e.g., “folder”, “file”, with simple metadata).
- Formatting preference update: for directory previews with arrows/descriptions, use **fixed-width alignment** (monospace/code block) so separators are visually sejajar.
- Final preference: use the **exact clean aligned template** as primary default for folder display ("yang ini aja").
- User explicitly requested reusable documentation for other AIs; created dedicated playbook with approved example layout so agents can replicate style in one read.
